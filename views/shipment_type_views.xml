<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="shipment_type_view_list" model="ir.ui.view">
        <field name="name">shipment.type.view.list</field>
        <field name="model">shipment.type</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="name"/>
                <field name="code"/>
                <field name="category"/>
            </list>
        </field>
    </record>

    <record id="shipment_type_view_form" model="ir.ui.view">
        <field name="name">shipment.type.view.form</field>
        <field name="model">shipment.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <group>
                        <field name="name"/>
                        <field name="code"/>
                        <field name="category"/>
                        <field name="active" widget="boolean_toggle"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="shipment_type_view_search" model="ir.ui.view">
        <field name="name">shipment.type.view.search</field>
        <field name="model">shipment.type</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="code"/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <group>
                    <filter string="Category" name="group_category" context="{'group_by': 'category'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_shipment_type" model="ir.actions.act_window">
        <field name="name">Shipment Types</field>
        <field name="res_model">shipment.type</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first shipment type
            </p>
        </field>
    </record>
</odoo>
